[pytest]
addopts = -q --cov=module_4/src --cov-report=term-missing --cov-fail-under=100 --strict-markers
markers =
    web: Flask page & routes
    buttons: Pull/Update endpoints + busy state
    analysis: labels, formatting (two decimals)
    db: schema/inserts/selects
    integration: end-to-end flow
